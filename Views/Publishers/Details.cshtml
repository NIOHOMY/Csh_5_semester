@model WebApplication1.Models.Publisher

@{
    ViewData["Title"] = "Детальная информация";
}

<head>
    <link rel="stylesheet" type="text/css" href="~/css/additional_styles.css">
</head>
<div class="text-center">
    <h4>Издательство</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.NameOfPublisher)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NameOfPublisher)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.City)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.City)
        </dd>
    </dl>
</div>
<div class="text-center">
    <a asp-action="Index">Назад</a>
    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
    {
        <text> | </text>
    <a asp-action="Edit" asp-route-id="@Model?.PublisherId">Редактировать</a> 
    }
</div>
